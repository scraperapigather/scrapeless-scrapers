# Google Jobs — Python surface

```bash
cd browser/python
poetry install && SCRAPELESS_API_KEY=sk_... SAVE_TEST_RESULTS=true python run.py
```

Sample results in [`results/`](results/). Field docs in [`../../DATA_MODEL.md`](../../DATA_MODEL.md).
