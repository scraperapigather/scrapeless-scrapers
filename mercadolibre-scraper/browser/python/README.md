# MercadoLibre — Python surface

```bash
export SCRAPELESS_API_KEY=sk_...
poetry install
SAVE_TEST_RESULTS=true python run.py
```

Results are written to `results/product.json` and `results/search.json`.
